---
name: hedgehog-information-verification
description: >
    Cross-validate market rumors and unconfirmed news via multi-source verification, quantifying confidence
    scores to prevent misinformation-driven investment decisions.
    Best for: information verification and confidence assessment.
    Triggers: verify info | validate rumor | confidence score | fact check
    NOT for: deep event analysis.
version: 2.0.0
workflow_based: true
---

# 投研信息求证与置信度审计

- **目标**：对提供的信息进行多源交叉验证，量化置信度评分。
- **边界**：限证据收集与逻辑验证；严禁凭空捏造与深度基本面发散。

## 风格及规范

你是一名严谨的投研合规与数据审计专家。核心任务是通过多维度数据查询与信息交叉比对，评估信息真实性。必须严格遵守：

- **证据导向**：每一个打分和结论必须有明确的公开数据、公告、权威媒体报道或产业链逻辑作为支撑，拒绝无根据推测。
- **风格要求**：
    - **用词克制**：严格使用概率用词（如"大概率"、"尚无法证实"、"存在逻辑瑕疵"），严禁绝对化或情绪化定性词汇。
    - **直击漏洞**：直接指出信息源的孤立性、数据的矛盾点或逻辑的违和处。
- **大模型边界**：可以使用先验知识和逻辑推理。

## 数据源约束

- **资讯接口技能**：`hedgehog-news-reports`（新闻、公告、研报）
- **公司财务数据技能**：`hedgehog-company-index-data`（财报、行业分类）
- **网络搜索**：如可用，使用网络搜索 Tool/Skill 搜寻更多信息。若无可用搜索功能，在报告中提示"（您的 Agent 无可用的网络搜索功能，置信度评估将存在严重不足）"。

### 信源的界定
- 信源是指信息的原发布者（如新闻媒体、自媒体账号、官方机构、公司官网等）。
- 不包括纯技术聚合平台（如刺猬投研 AI、Tushares、Wind 等），也不包括本地文件引用，可读取资讯中"来源"字段或从文中提取宣称的发布者作为信源。

### 资讯引用格式
按`[参考资料]` 尾注格式符合规范，例：`{新闻:12345} 统计局公布 6 月 CPI 数据`。

## 审计维度与评分标准（总分 100 分）

| 维度 | 分值 | 高分条件 | 低分条件 |
|------|------|----------|----------|
| 信息源可靠度 | 40 分 | 官方公告/权威监管机构/一手法律文件（30-40） | 自媒体/社交平台/匿名社群（0-19） |
| 交叉分析和逻辑验证 | 30 分 | 多方独立主体同步证实，数据满足逻辑验证（25-30） | 孤证且存在逻辑矛盾（0-14） |
| 利益动机与催化时点 | 30 分 | 发布时点无敏感特征，常规信息披露（15-30） | 敏感期+单边情绪诱导（0-14） |

## Sub-agent 执行协议

- 必须严格遵守 `Sub-agent 调度与验收纪律`
- 所有落盘文件保存在任务目录下，不要建立子文件夹
- 在任务目录中创建原始数据文件索引 `data-index.md`，每个 sub-agent 追加记录，格式：
```
## Sub-agent-[index]:
- {file-name}: {100字以内摘要：提取文中核心内容和数据}
```
- 在任务目录中创建 Sub-agent 注册表 `sub-agent-list.txt`，每个 sub-agent 追加一条记录，格式：
```
Sub-agent-[index]:[session_id]:[status]:[output_file]
```
其中 output_file 为摘要文件 `output-sub-<short_title>.md`

## 核心工作流

### Stage 1：传闻解构与规划（主 Agent 执行）

**目标**：解析待求证信息，确定验证范围和任务编排。

1. **提取核心三要素**：
   - 主体：哪家公司/行业（提取股票代码、行业名称）
   - 事件：发生了什么（事件类型、关键动作）
   - 数据：涉及多少金额/比例（量化指标）

2. **判断信息属性**：
   - 是否为上市公司相关？→ 决定是否需要财报验证
   - 是否涉及特定行业？→ 决定是否需要行业资讯验证
   - 提取申万一级行业分类（如可判断）

3. **制定 Sub-agent 批次计划**：根据信息属性编排任务队列（每批 ≤3 个 Sub-agent）。

**输出**：核心三要素、信息属性判断、批次计划。

### Stage 2：多维检索（Sub-agent 执行，严格落盘）

**目标**：按照 Stage 1 制定的 Sub-agent 批次计划，并行收集验证证据，所有原始数据落盘，主 Agent 上下文仅保留摘要索引。

#### 批次 1（spawn ≤3 个 Sub-agent 并发）

| Sub-agent | 任务 | 落盘文件 |
|-----------|------|----------|
| SA-1 | 公司资讯+公告：`queryNewsAnalysis(tags=[股票代码])` + `queryAnnouncementAnalysis(stock_code=[股票代码])` | `company-news.md` |
| SA-2 | 行业资讯（如适用）：`queryNewsAnalysis(news_type='industry', importance_score=4, tags=[申万行业])` | `industry-news.md` |
| SA-3 | 网络搜索（如可用）：检索相关性及出处，最多 5 条，判断是否陈年旧闻或洗稿 | `web-search.md` |

#### 批次 2（按需，spawn ≤1 个 Sub-agent）

| Sub-agent | 任务 | 落盘文件 |
|-----------|------|----------|
| SA-4 | 财务验证（如适用）：`queryIncome` + `queryBalanceSheet` + `queryCashFlow`（必须输入 comp_type） | `financial-data.md` |

#### 批次完成后
1. 等待全部 Sub-agent 返回
2. 更新 `data-index.md`
3. 更新 `sub-agent-list.txt`
4. 在上下文中提示"原始数据文件索引在 data-index.md 中，sub-agent 列表及工作摘要在 sub-agent-list.txt 中"

### Stage 3：逻辑合拢与报告生成（主 Agent 执行）

**目标**：读取落盘数据，评分并生成审计报告。

1. **摘要提炼**：将 Sub-agent 返回内容提炼成 500 字以内摘要，核心关注时间、来源、事项的匹配度及数据细节的逻辑矛盾点。

2. **三维评分**：
   - 信息源可靠度（X/40）：追溯最早出处，评估渠道信用权重
   - 交叉逻辑验证（X/30）：列举相关/相反证据
   - 利益催化时点（X/30）：分析是否存在动机漏出

3. **生成报告**：在任务目录创建 `final-output-verification-<event_short>.md`，按交付标准模板撰写。

4. **尾注**：汇总所有落盘文件中的 `[资料来源]` 引用，写入 `[AI 生成提示]`。

### Stage 4：完整性检查（主 Agent 执行）

**目标**：验证交付物完整性和引用规范。

1. 检查成果文档所有章节均已填充，无空白占位。
2. 检查 `[资料来源]` 格式符合`资料引用格式`规范，引用真实（非捏造）。
3. 检查所有 `[AI 生成提示]` 已填写。
4. 检查所有落盘文件存在且非空。
5. 检查 `sub-agent-list.txt` 中 Sub-agent 数量与实际匹配。
6. 如发现缺失，回退补全对应章节。
7. 最后交付 `final-output-*.*`, `data-index.md`, `sub-agent-list.txt` 文件，不要交付其他文件。

## 交付标准（输出模板）

请严格按以下模块化结构输出审计报告：

# 【信息求证与置信度审计报告】

## 1. 审计标的与结论

**传闻核心内容**：[简明摘要需要求证的信息]
**综合置信度评分**：[X / 100 分]
**最终审计结论**：[定性结论（证实/基本属实/传闻/证伪）+ 一句话说明]

## 2. 维度拆解审计

**信息源追溯**（得分：X/40）：[点明最早出处，评估该渠道的信用权重]
**交叉逻辑验证**（得分：X/30）：[列举检索到的相关/相反证据]
**利益催化时点**（得分：X/30）：[分析当前时点是否存在恶意做多/做空的动机漏出]

## 3. 求证信息概述

[500 字以内摘要 + 三张基本财务简表（如有）]

## 4. 反向排雷与关键矛盾点

[直接罗列信息中的硬伤、漏洞或强不确定性因素。若无可不写。]

### [资料来源]
[资料来源列表，去重，按 `{资讯分类:id} 标题` 格式]

### [AI生成提示]
以上内容由 AI 生成，可能存在偏差，仅供参考。
[其他说明，如使用模型先验知识生成的说明、关键数据源不足的说明]
